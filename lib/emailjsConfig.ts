// EmailJS lets the browser send a real email directly to the site
// owner's inbox without any server-side code or exposing the owner's
// actual email address anywhere in this codebase — only these three
// IDs are needed client-side, and EmailJS's public key is specifically
// designed to be safe in frontend code (unlike the private key, which
// must never appear here).
export const EMAILJS_CONFIG = {
  serviceId: "service_nmxsfh8",
  templateId: "template_7tg4kbe",
  publicKey: "fglutleVgCqfqQBJE",
};
