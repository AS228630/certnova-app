# راهنمای پروژه‌ی CertCoach — سند زنده‌ی وضعیت پروژه

> **این فایل رو کامل کپی کن و اولین پیام هر جلسه‌ی کاری جدید بفرست** تا فوراً همه‌چیز فهمیده بشه و بدون سؤال اضافه شروع بشه.
>
> **قانون مهم برای هر جلسه:** در پایان کار، این فایل رو با تغییرات جدید به‌روزرسانی کن (بخش «تاریخچه‌ی تغییرات» رو با یه ورودی جدید تکمیل کن) و commit/push کن. این‌طوری هیچ‌وقت زمینه‌ی کار از دست نمی‌ره.

**آخرین به‌روزرسانی:** ۱۷ ژوئیه ۲۰۲۶

---

## ۱. معرفی پروژه

| | |
|---|---|
| **نام** | CertCoach — پلتفرم آموزش آنلاین گواهینامه‌های IT، دوره‌های زبان، و آماده‌سازی مصاحبه |
| **دامنه‌ی زنده** | https://www.certcoach.de |
| **مخزن گیت‌هاب** | https://github.com/AS228630/certnova-app (Public) |
| **مالک** | Ataullah Senmas — Mudweg 17, 63897 Miltenberg, Deutschland |
| **ایمیل عمومی** | info@certcoach.de |
| **زبان‌های سایت (۹ زبان)** | آلمانی (پیش‌فرض)، انگلیسی، فارسی، عربی، اوکراینی، اسپانیایی، فرانسوی، روسی، ترکی |

---

## ۲. پشته‌ی فنی (Tech Stack)

| بخش | سرویس/فناوری |
|---|---|
| فریم‌ورک | Next.js 16 (App Router)، TypeScript، Tailwind CSS |
| مدیریت state | Zustand |
| احراز هویت + دیتابیس | **Supabase** (PostgreSQL + Auth + RLS) |
| هاستینگ/دیپلوی | **Vercel** (هر push به شاخه‌ی `main` خودکار دیپلوی می‌شه، ۱-۲ دقیقه طول می‌کشه) |
| آنالیتیکس | **Vercel Web Analytics** (بدون کوکی، بدون نیاز به بنر رضایت) |
| پرداخت | **Stripe Checkout** (کارت + PayPal + Klarna — همه از طریق یک ادغام) |
| ایمیل فرم تماس | **EmailJS** (کلاینت‌ساید، بدون سرور جدا) |
| هوش مصنوعی (KI Coach) | Gemini API یا Azure OpenAI (هرکدوم که `GEMINI_API_KEY` یا متغیرهای Azure تنظیم شده باشه) |

---

## ۳. محیط کاری توسعه (Sandbox)

- محیط کاری یک کامپیوتر لینوکسی جدا با دسترسی اجرای دستورات (bash) داره.
- مخزن پروژه رو باید هر بار از گیت‌هاب clone کرد (چون هر sandbox جدید خالیه): `git clone https://github.com/AS228630/certnova-app.git`
- محدودیت شبکه: فقط به چند دامنه‌ی خاص (`github.com`, `npmjs.com`, `pypi.org` و مشابه) دسترسی هست — نمی‌شه مستقیم به Stripe، Supabase، یا خودِ certcoach.de وصل شد.
- فایل‌های آپلودی کاربر (عکس‌ها، PDF) در `/mnt/user-data/uploads/` قرار می‌گیرن.
- فایل‌های خروجی (برای دانلود کاربر) باید در `/mnt/user-data/outputs/` کپی بشن.

---

## ۴. نحوه‌ی push کردن تغییرات به گیت‌هاب — ⚠️ نکته‌ی امنیتی حیاتی

این محیط به‌تنهایی دسترسی push دائمی به گیت‌هاب نداره — نیاز به یک **GitHub Personal Access Token** داره که کاربر (شما) هر بار باید در چت وارد کنه:

```bash
git push https://<TOKEN>@github.com/AS228630/certnova-app.git main
```

### 🔴 قانون امنیتی که باید همیشه رعایت بشه:
**بعد از هر بار استفاده از یک توکن در چت، حتماً و بدون استثنا باید بلافاصله در `github.com/settings/tokens` آن توکن Revoke (باطل) بشه و برای push بعدی یک توکن تازه ساخته بشه.**

چرا این‌قدر مهمه: توکن همین‌که در متن چت تایپ می‌شه، دیگه «مخفی» نیست — تو تاریخچه‌ی چت باقی می‌مونه. در طول این پروژه **چندین بار** توکن‌ها بدون باطل شدن دوباره استفاده شدن (یه ریسک امنیتی واقعی که هنوز کامل رعایت نشده). هر چت جدید باید این هشدار رو به‌وضوح تکرار کنه.

### قدم‌های استاندارد قبل از هر push:
1. `npx tsc --noEmit` (بررسی خطای TypeScript)
2. `npx eslint <فایل‌های تغییریافته>` (بررسی سبک کد)
3. Build کامل با متغیرهای placeholder:
   ```bash
   cat > .env.local << 'EOF'
   NEXT_PUBLIC_SUPABASE_URL=https://placeholder.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=placeholder-anon-key
   EOF
   npm run build
   rm -f .env.local
   ```
4. فقط اگر هر سه مرحله بدون خطا بود: `git add -A && git commit -m "..." && git push ...`

### نکته‌ی فنی مهم درباره‌ی API Routes:
هرگز کلاینت‌های سرویس‌های خارجی (Stripe، Supabase Admin) رو در سطح بالای فایل نسازید — همیشه باید داخل تابع handler (lazy) ساخته بشن، وگرنه build کامل next خراب می‌شه چون متغیرهای محیطی در آن لحظه در دسترس نیستن.

---

## ۵. پایگاه‌داده (Supabase) — نکته‌ی حیاتی

این محیط به هیچ‌وجه دسترسی مستقیم SQL به Supabase نداره — نمی‌شه جدول ساخت یا migration رو خودکار اجرا کرد. روند کار صحیح:

1. یک فایل migration جدید در `supabase/migrations/0XX_نام.sql` ساخته می‌شه (با شماره‌ی بعدی).
2. فایل رو در `/mnt/user-data/outputs/` کپی و برای دانلود آماده می‌شه.
3. **کاربر باید خودش** به `https://supabase.com/dashboard` بره → پروژه رو باز کنه → SQL Editor → New query → محتوا رو پیست کند → Run بزنه.
4. همیشه باید مطمئن شد کاربر واقعاً migration رو اجرا کرده، قبل از فرض کردن که یک جدول/ستون در دیتابیس وجود داره.

### جدول‌های موجود (تا این لحظه):
`user_topic_mastery`, `user_activity_log`, `support_messages`, `webinar_signups`, `newsletter_subscribers`, `subscriptions`, `user_cert_progress`, `user_lesson_completions`, `percert_lab_practice`, `profiles`, `exam_questions`, `ai_conversations`, `ai_messages`, `interview_prep`, `language_course_progress`, `community` (posts/likes/comments)`, `live_study_rooms`, `activity_read_state`, **`user_question_answers`** (جدید — پاسخ تک‌تک سؤالات تمرینی، برای حل‌شدن باگ قفل‌شدن بخش‌ها؛ **باید توسط کاربر اجرا بشه، فایل: `supabase/migrations/017_user_question_answers.sql`**)

### مدیریت فضای دیتابیس (پلن رایگان Supabase = سقف ۵۰۰ مگابایت)

برای این‌که در آینده به مشکل فضا نخوریم، این اصل همیشه باید رعایت بشه:

- **داده‌ی ماندگار و باارزش کسب‌وکاری** (پروفایل کاربر، پیشرفت واقعی دوره/آزمون، اشتراک/پرداخت، پست‌های Community) → همیشه در جدول PostgreSQL واقعی ذخیره بشه (روش فعلی، درسته).
- **داده‌ی کاملاً موقتی و بدون نیاز به تاریخچه** (مثلاً وضعیت «کی الان آنلاینه»، نشانگر «در حال تایپ»، سیگنال‌های لحظه‌ای بین دو کاربر) → باید از **Supabase Realtime Presence/Broadcast** استفاده بشه، نه `insert()`. این‌ها هرگز روی دیسک ذخیره نمی‌شن، فقط در حافظه:

```js
// Presence — فقط در حافظه، هرگز روی دیسک
const channel = supabase.channel('live-room-123', {
  config: { presence: { key: 'user_session' } }
});
channel.track({ user: 'Ataullah', online_at: new Date() });

// Broadcast — پیام لحظه‌ای بدون ذخیره‌سازی
channel.send({ type: 'broadcast', event: 'chat-message', payload: {...} });
```

**نکته‌ی مهم:** چت اتاق‌های Live Coach عمداً در جدول واقعی ذخیره می‌شه (نه فقط Broadcast)، چون کاربر باید بتونه تاریخچه‌ی پیام‌ها رو دوباره ببینه. این یه تصمیم آگاهانه است، نه فراموشی این اصل.

---

## ۶. سیستم پرداخت Stripe — وضعیت فعلی

- کد سیستم پرداخت **کاملاً پیاده‌سازی شده** و امنه (checkout session، webhook، RLS، lazy client init — همه درست).
- پشتیبانی از Card + PayPal + Klarna از طریق یک ادغام Stripe واحد (نیازی به SDK جدا نیست).
- **کلید فعلی هنوز `pk_test_...` است — یعنی سایت هنوز روی Test mode است، نه Live.** برای پول واقعی، کاربر باید در Stripe Dashboard حساب رو Verify کنه و به Live mode سوییچ کنه.
- **PayPal تو صفحه‌ی پرداخت نمایش داده نمی‌شه** — چون تو Stripe Dashboard (نه در کد) فعال نشده. کاربر باید بره Settings → Payment methods و PayPal رو فعال کنه.
- کلید عمومی (Publishable Key) در `lib/stripeConfig.ts` امن است چون طراحی شده برای فرانت‌اند. کلیدهای مخفی (Secret Key، Webhook Secret، Supabase Service Role Key) فقط در Vercel Environment Variables هستن، هرگز در کد.

### دکمه‌ی کنسل اشتراک (قانونی، الزامی طبق § 312k BGB آلمان):
صفحه‌ی `/kuendigen` ساخته شده — همیشه در دسترس، بدون نیاز به لاگین، لینک شده تو فوتر. اشتراک رو واقعاً در Stripe لغو می‌کنه (نه فقط ظاهری).

---

## ۷. وضعیت حقوقی — همه‌ی موارد حل شده ✅

| صفحه | وضعیت |
|---|---|
| Impressum | ✅ کامل، با اطلاعات واقعی |
| Datenschutz (حریم خصوصی) | ✅ کامل — همه‌ی ۵ مورد placeholder که قبلاً زرد و قابل‌مشاهده برای کاربر بودن، با اطلاعات واقعی و تأییدشده پر شدن (Vercel SCC، Supabase DPA، Stripe به‌عنوان پردازشگر پرداخت، Vercel Analytics بدون کوکی) |
| AGB (شرایط استفاده) | ✅ کامل نوشته شد (قبلاً فقط «به‌زودی» بود) — ۱۲ بخش، شامل قیمت، فسخ، مسئولیت |
| Widerrufsrecht (حق انصراف) | ✅ کامل — شامل بند § 356 Abs. 4 BGB (پایان زودهنگام حق انصراف برای خدمات دیجیتال) + فرم نمونه‌ی انصراف + چک‌باکس رضایت اجباری در چک‌اوت (با تاریخ/زمان در Stripe metadata ثبت می‌شه) |
| دکمه‌ی کنسل (§ 312k BGB) | ✅ صفحه‌ی `/kuendigen` ساخته شد |
| Barrierefreiheit / Sicherheit | ✅ صادقانه نوشته شدن (بدون ادعای دروغ درباره‌ی گواهی امنیتی یا سازگاری کامل با WCAG) |

**تنها توصیه‌ی باقی‌مونده:** قبل از فعال کردن نهایی پرداخت واقعی (Live mode)، یک‌بار این متون رو با یک وکیل آلمانی (یا سرویس آنلاین مثل eRecht24) چک کنید — این توصیه‌ی احتیاطی است، نه یک مشکل شناخته‌شده.

---

## ۸. وضعیت امنیتی — مشکلات پیدا و حل‌شده

- ✅ **API چت KI Coach** قبلاً هیچ احراز هویتی نداشت (هر کسی بدون لاگین می‌تونست ازش استفاده و سهمیه‌ی پولی API رو هدر بده) — الان نیاز به توکن کاربر واقعی داره + محدودیت ۶۰ پیام در ساعت.
- ✅ هدرهای امنیتی استاندارد (`X-Frame-Options`, `X-Content-Type-Options`, `Strict-Transport-Security`, `Referrer-Policy`, `Permissions-Policy`) به `next.config.ts` اضافه شدن.
- ⚠️ **Content-Security-Policy (CSP) هنوز اضافه نشده** — عمداً به تعویق افتاده چون نیاز به فهرست دقیق تمام دامنه‌های خارجی (Vercel Analytics، Stripe، flagcdn.com، Supabase، EmailJS) داره تا چیزی رو خراب نکنه. یک قدم بعدی مشخص برای آینده.
- ✅ صفحه‌ی ۴۰۴ سفارشی (`app/not-found.tsx`) و error boundary (`app/global-error.tsx`) اضافه شدن — قبلاً صفحه‌ی پیش‌فرض بی‌هویت Next.js نمایش داده می‌شد.
- 🔴 **هیچ کلید مخفی در خودِ کد کامیت نشده** (چک شده، تمیزه) — اما توکن‌های گیت‌هاب چندین بار در متن چت افشا شدن. این نیازمند رعایت دقیق‌تر قانون بخش ۴ در آینده است.

---

## ۹. وضعیت محتوا (بزرگ‌ترین کمبود پروژه برای رقابت واقعی)

- **۴۲ گواهینامه، ۲۱ شرکت** در کاتالوگ هست، اما فقط **۳ گواهینامه محتوای واقعی و کامل** دارن:

| گواهینامه | بانک سؤال | ماژول آموزشی | لابراتوار عملی |
|---|---|---|---|
| AZ-900 (Azure Fundamentals) | ✅ ۵۶۴ سؤال واقعی | ✅ | ✅ |
| AZ-104 (Azure Administrator) | جزئی | ✅ | ✅ |
| AB-900 (M365 Copilot) | ✅ ۱۰۱ سؤال واقعی | ❌ (هنوز نداره) | ❌ |

- بقیه‌ی ~۳۹ گواهینامه محتوای عمومی/جایگزین (generated) دارن — نوشتن محتوای واقعی برای همه‌ی این‌ها یک پروژه‌ی بزرگ و جدا است (احتمالاً ماه‌ها کار).
- زبان‌ها (Sprachkurse): چارچوب و ردیابی پیشرفت واقعی آماده‌ست، اما **هیچ محتوای واقعی درسی هنوز نوشته نشده** برای هیچ‌کدوم از ۸+ زبان — این هم یک پروژه‌ی بزرگ جدا.

### بقیه‌ی ویژگی‌های واقعی سایت (خارج از بحث گواهینامه‌ها)

| ویژگی | وضعیت واقعی (بررسی‌شده مستقیم از کد) |
|---|---|
| Community | ✅ واقعی — پست/لایک/کامنت به دیتابیس نوشته می‌شه؛ چند نمونه‌ی تستیمونیال با برچسب صادقانه‌ی «Beispiel» مشخص شدن |
| Live Coach — چت متنی | ✅ واقعی، از طریق Supabase Realtime |
| Live Coach — تماس تصویری/صوتی | ❌ **فعلاً عمداً غیرفعال** و صادقانه «به‌زودی» نشون داده می‌شه. (نکته: یک نسخه‌ی قدیمی‌تر این راهنما ادعا کرده بود تماس تصویری از طریق Jitsi رایگان واقعی و فعاله — این دیگه درست نیست؛ طبق کامنت مستقیم تو کد `app/coach-live/page.tsx`، تمام گزینه‌های رایگان تست‌شده [Jitsi عمومی، MiroTalk P2P، 8x8، Matrix/Element Call] برای گروه‌های ۲۰-۳۰+ نفره یا نیاز به پلن پولی داشتن، یا نیاز به سرور اختصاصی که هنوز نداریم، یا در این مقیاس ناپایدار بودن. به‌جای ارائه‌ی یه قابلیت ناپایدار یا با برند شخص ثالث، صادقانه «به‌زودی» نشون داده می‌شه.) |

**گزینه‌ی آینده برای تماس تصویری کاملاً مستقل:** اگه روزی خواستید این رو راه بندازید، بهترین مسیر شناسایی‌شده: یک سرور Oracle Cloud Always Free Tier (رایگان دائمی، نه دوره‌ی آزمایشی؛ ۴ هسته + ۲۴ گیگ رم) + نصب LiveKit (متن‌باز) روی اون + دامنه‌ی اختصاصی (مثل `meet.certcoach.de`) + اتصال مستقیم فرانت‌اند با `@livekit/react-components` (بدون iframe، بدون برند خارجی). این یک پروژه‌ی جداگانه است (نیاز به راه‌اندازی واقعی DNS/SSL/Docker)، نه یک تغییر سریع کد.

### وضعیت ترجمه‌ی بانک سؤالات (تنها ۲ گواهینامه‌ای که بانک سؤال واقعی دارن):

| گواهینامه | آلمانی (مرجع) | انگلیسی | فارسی | بقیه‌ی ۶ زبان |
|---|---|---|---|---|
| AZ-900 (۵۶۴ سؤال) | ✅ ۱۰۰٪ | فقط ۱۶ سؤال (۳٪) | ۴۰ از ۵۶۴ (اولین قسط) | ❌ صفر |
| AB-900 (۱۰۱ سؤال) | ✅ ۱۰۰٪ | ✅ ۱۰۰٪ | ✅ ۱۰۰٪ (۱۰۱ از ۱۰۱) | عربی: ۵۰ از ۱۰۱ (اولین قسط) |

فایل‌های ترجمه در `lib/i18n/questions/<cert>.<locale>.ts` — سیستم به‌گونه‌ای طراحی شده که **ترجمه‌ی ناقص هیچ مشکلی ایجاد نمی‌کنه**: هر سؤالی که هنوز ترجمه نشده، خودکار به آلمانی نمایش داده می‌شه (fallback امن).

---

## ۱۰. تاریخچه‌ی کامل مشکلات پیدا و حل‌شده (این جلسه، ۱۷ ژوئیه ۲۰۲۶)

### حقوقی و امنیتی
1. Widerrufsrecht ناقص → متن کامل حقوقی + فرم نمونه + چک‌باکس رضایت
2. دکمه‌ی کنسل اشتراک (§ 312k) اصلاً وجود نداشت → صفحه‌ی `/kuendigen` ساخته شد
3. ۵ مورد placeholder زرد‌رنگ روی صفحه‌ی زنده‌ی Datenschutz (قابل‌مشاهده برای هر کاربر!) → با اطلاعات واقعی پر شدن
4. AGB اصلاً نوشته نشده بود (فقط «به‌زودی») → کامل نوشته شد
5. API چت KI Coach بدون احراز هویت بود → نیاز به لاگین + rate limit اضافه شد
6. بدون هدر امنیتی در next.config → اضافه شدن
7. بدون صفحه‌ی ۴۰۴/error سفارشی → اضافه شدن

### باگ‌های عملکردی
8. صفحه‌ی جزئیات دوره‌ی زبان (`/language-courses/[slug]`) اصلاً وجود نداشت → همه‌ی دکمه‌های «ادامه» ۴۰۴ می‌دادن → صفحه‌ی صادقانه‌ی «در حال ساخت» جایگزین شد
9. ۲ لینک مرده (`href="#"`) → یکی به داشبورد وصل شد، یکی (تزئینی تو شبیه‌سازی Azure) از پرش صفحه جلوگیری شد
10. **باگ بحرانی: پیشرفت بخش‌های تمرین (section unlock) هرگز در دیتابیس ذخیره نمی‌شد** — فقط در حافظه‌ی مرورگر بود و با بستن سایت پاک می‌شد → جدول `user_question_answers` + استور جدید ساخته شد، پاسخ‌ها حالا دائمی ذخیره می‌شن
11. متن سؤالات به‌شکل یک پاراگراف پیوسته نمایش داده می‌شد (بدون فاصله‌بندی pdf) → `whitespace-pre-line` اضافه شد
12. فونت گزینه‌های جواب خیلی کوچیک بود (۱۴px) → به ۱۶px افزایش یافت
13. سؤالات چندجوابی («select all that apply») فقط اجازه‌ی انتخاب یک گزینه رو می‌دادن → پشتیبانی کامل چک‌باکس/چندانتخابی اضافه شد
14. **بررسی خودکار هر ۵۶۴ سؤال AZ-900 در برابر PDF اصلی** → ۲۶ سؤال پیدا شد که واقعاً ۲-۳ جواب درست داشتن ولی فقط یکی ثبت شده بود → همه اصلاح شدن؛ توضیحات (Erläuterung) صفر مورد خالی داشتن
15. سؤالات تطبیقی (matching): ترتیب نمایش گزینه‌ها همیشه دقیقاً روبه‌روی جواب درستشون بود (قابل‌حدس بدون خوندن) → shuffle پایدار اضافه شد

### طراحی و UI (پیش‌تر در همین جلسه)
16. پنجره‌ی چهارم «درباره‌ی امتحان» + صفحه‌ی شبیه‌سازی امتحان کامل (`/mock-exam`) ساخته شد — تایمر واقعی، حالت تمام‌صفحه، صفحه‌ی مرور، نتیجه
17. طراحی ۴ کارت هم‌اندازه با CSS Grid (قبلاً با flex می‌شکست)
18. ترجمه‌ی ۲۸ سؤال اول AB-900 به فارسی (ادامه دارد)

### این جلسه (۱۹ ژوئیه ۲۰۲۶)
19. **مالک پروژه migration `017_user_question_answers.sql` رو در Supabase اجرا کرد** — باگ قفل‌شدن دوباره‌ی بخش‌ها بعد از بستن مرورگر تأیید شد که کاملاً حل شده
20. **استراتژی «۶ گواهینامه‌ی اصلی»** — طبق پیشنهاد یک برنامه‌نویس دیگه، فقط ۶ شرکت (Microsoft, AWS, Cisco, CompTIA, Linux, ITIL) الان تو کل سایت (گرید، کاروسل، جست‌وجو، ویجت پیشنهادها، sitemap) دیده می‌شن. ۱۵ شرکت دیگه **کاملاً مخفی**‌ان (نه فقط قفل با نشون دادن) ولی داده‌هاشون دست‌نخورده تو `lib/companiesData.ts` مونده — باز کردنشون بعداً فقط یه فلگ `featured: true` می‌خواد.
21. **شروع اصلاح سئوی بین‌المللی** — مشکل: دوستِ کاربر در افغانستان با جست‌وجوی نام سایت، پیداش نمی‌کرد (ولی در آلمان پیدا می‌شد). علت واقعی پیدا شد: سایت هیچ URL جدا برای هر زبون نداشت (فقط سوییچ زبان سمت کلاینت/جاوااسکریپت بود)، پس گوگل فقط نسخه‌ی آلمانی رو می‌دید. **قدم اول** انجام شد: صفحات واقعی و سمت‌سرور `certcoach.de/en` و `certcoach.de/fa` ساخته شدن (با محتوای واقعی از دیکشنری‌های موجود سایت، نه چیز تازه)، با تگ‌های `hreflang` متقابل و اضافه‌شدن به sitemap. **هنوز ناقصه** — فقط صفحه‌ی اصلی، نه کل سایت (۳۰+ صفحه، ۹ زبون)؛ قدم بعدی: مالک پروژه باید تو Google Search Console درخواست ایندکس فوری برای `/en` و `/fa` بده.

---

## ۱۱. قدم‌های باقی‌مانده (اولویت‌بندی‌شده)

### 🔴 فوری — فقط از طریق اقدام خودِ کاربر (نه کد)
- [x] اجرای migration جدید `017_user_question_answers.sql` در Supabase SQL Editor — ✅ انجام شد، تأیید شد که کار می‌کنه
- [ ] **درخواست ایندکس فوری در Google Search Console** برای `certcoach.de/en` و `certcoach.de/fa` (بخش URL-Prüfung → Indexierung beantragen) — تنها راه تسریع ایندکس‌شدن سئوی بین‌المللی
- [ ] فعال کردن PayPal در Stripe Dashboard → Settings → Payment methods
- [ ] سوییچ از Test mode به Live mode در Stripe (نیاز به Verify کردن کسب‌وکار)
- [ ] باطل کردن هر توکن گیت‌هابی که تا الان در چت استفاده شده (اگه هنوز نشده)

### 🟡 محتوایی — پروژه‌های بزرگ، قدم‌به‌قدم

**برنامه‌ی ترجمه (طبق تصمیم مالک پروژه: تکمیل تدریجی همه‌ی ۹ زبون، یک دسته در هر جلسه):**
1. ✅ AB-900 → آلمانی، انگلیسی، فارسی (۱۰۰٪ هرکدوم)
2. 🔄 AB-900 → عربی (۵۰ از ۱۰۱، در حال انجام)
3. ⏳ AB-900 → اوکراینی، اسپانیایی، فرانسوی، روسی، ترکی (هنوز شروع نشده)
4. 🔄 AZ-900 → انگلیسی (۱۶ از ۵۶۴)، فارسی (۴۰ از ۵۶۴) — هر دو در حال انجام
5. ⏳ AZ-900 → عربی، اوکراینی، اسپانیایی، فرانسوی، روسی، ترکی (هنوز شروع نشده)

- [ ] گسترش سئوی چندزبانه به بقیه‌ی ۷ زبون (عربی، اوکراینی، اسپانیایی، فرانسوی، روسی، ترکی) و بقیه‌ی صفحات مهم سایت (نه فقط صفحه‌ی اصلی)
- [ ] ادامه‌ی ترجمه‌ی AB-900 به عربی (۵۱ سؤال باقی‌مونده از ۱۰۱ — سؤالات ۱-۵۰ تو این جلسه انجام شد)
- [ ] ادامه‌ی ترجمه‌ی AZ-900 به فارسی (۵۲۴ سؤال باقی‌مونده از ۵۶۴)
- [ ] تکمیل ترجمه‌ی AZ-900 به انگلیسی (۵۴۸ سؤال باقی‌مونده از ۵۶۴)
- [ ] محتوای واقعی (ماژول + لابراتوار + بانک سؤال) برای هر ۶ گواهینامه‌ی فعلاً فعال (Microsoft تنها کاملشه)
- [ ] محتوای واقعی درسی برای دوره‌های زبان (فعلاً فقط چارچوب آماده‌ست)
- [ ] **پنل ادمین** برای افزودن سؤالات جدید بدون نیاز به ویرایش کد/دیپلوی مجدد (پیشنهاد یک برنامه‌نویس دیگه — بحث شد، هنوز شروع نشده)

### 🟢 بهبودهای فنی جزئی (اختیاری)
- [ ] افزودن Content-Security-Policy به next.config.ts (نیاز به فهرست دقیق دامنه‌های خارجی)
- [ ] گسترش rate-limit جدید KI Coach به modal های کوچیک‌تر (مصاحبه‌ی آزمایشی، کمک سریع)

---

## ۱۲. اصل بنیادین پروژه — هیچ داده‌ی جعلی

این مهم‌ترین قانون کل پروژه است و باید در هر تصمیمی رعایت بشه:
- آمار پیشرفت کاربر همیشه باید از دیتابیس واقعی خونده بشه، هرگز عدد ساختگی.
- محتوای آموزشی هرگز نباید از سایت‌های شخص ثالث کپی بشه.
- اگه بخشی از یک ویژگی هنوز ساخته نشده، باید صادقانه «به‌زودی» یا حالت خالی نشون بده — نه محتوای جعلی.
- صفحات حقوقی فقط با اطلاعات واقعی که کاربر می‌ده پر بشن.
- قبل از نوشتن کد یا ساخت فایل، فایل‌های `SKILL.md` مرتبط در `/mnt/skills/public/` باید خونده بشن.

---

## ۱۳. پروژه‌ی جدید و جدا: فروشگاه آنلاین Outlet Möbel GmbH

> ⚠️ این یک پروژه‌ی **کاملاً جدا** از CertCoach است — کد جدا، مخزن گیت‌هاب جدا، دیتابیس جدا. این بخش فقط برای این‌جاست که در چت‌های بعدی فراموش نشه.

**وضعیت CertCoach:** کار دوباره از سر گرفته شد (۱۹ ژوئیه ۲۰۲۶) — استراتژی «۶ گواهینامه‌ی اصلی» و شروع اصلاح سئوی بین‌المللی انجام شد (بخش ۱۰، ردیف‌های ۲۰-۲۱). این پروژه‌ی Outlet Möbel هم‌زمان و جدا ادامه داره.

### معرفی پروژه‌ی جدید
یک فروشگاه آنلاین مبلمان برای شرکت واقعی **Outlet Möbel GmbH** (آدرس واقعی: Weckbacherstrasse 12, 63937 Wertheim) در سطح رقبای بزرگ آلمانی مثل Otto، XXXLutz، و Höffner. هدف: فروشگاهی مدرن، سریع، سئو‌دوست و مقیاس‌پذیر.

### پشته‌ی فنی تصمیم‌گرفته‌شده

| بخش | فناوری | یادداشت |
|---|---|---|
| فریم‌ورک | Next.js (App Router) + React + TypeScript | |
| استایل | Tailwind CSS + shadcn/ui | برای کیفیت بالای کامپوننت‌ها (دکمه، دیالوگ، فرم، جدول و...) |
| انیمیشن | Framer Motion | برای Hero، Hover، Scroll Animation |
| بک‌اند | Next.js Server Actions | (نه NestJS جدا — تصمیم گرفته شد که برای MVP سادگی مهم‌تره؛ NestJS فقط اگه واقعاً به مقیاس خیلی بزرگ رسیدیم) |
| دیتابیس | PostgreSQL + Prisma (ORM) | |
| احراز هویت | Auth.js یا Clerk | هنوز قطعی نشده |
| پرداخت | **Stripe** (پوشش کارت + PayPal + Klarna با یک ادغام، طبق تجربه‌ی موفق CertCoach) | Apple Pay / Google Pay هم از طریق Stripe پوشش داده می‌شه |
| جست‌وجو | Algolia (ترجیح داده شد به ElasticSearch برای سادگی راه‌اندازی اولیه) | فقط اگه لازم شد اضافه بشه، نه از روز اول |
| کش | Redis | فقط اگه به مشکل کارایی واقعی خوردیم، نه از روز اول |
| CMS محصولات | Payload CMS یا Sanity | هنوز قطعی نشده |
| دیپلوی فرانت‌اند | Vercel | |
| دیپلوی بک‌اند (در صورت جدا شدن) | Railway یا Hetzner | |

### هویت بصری (از روی لوگوی واقعی شرکت استخراج شده)
- رنگ اصلی (قرمز): `#C31924`
- تیره: `#111111`
- متن: `#2B2B2B`
- خاکستری: `#6B7280`
- خاکستری روشن: `#F5F5F5`
- پس‌زمینه: `#FFFFFF`
- حاشیه: `#E5E7EB`
- موفقیت: `#22C55E`
- هشدار: `#F59E0B`
- فونت تیتر: Manrope (وزن ۷۰۰-۸۰۰)
- فونت متن: Inter (وزن ۴۰۰-۶۰۰)
- Border radius: دکمه ۱۴px، کارت ۲۰px، اینپوت ۱۲px
- سیستم فاصله‌گذاری: 8pt Grid (۸، ۱۶، ۲۴، ۳۲، ۴۰، ۴۸، ۶۴، ۸۰، ۹۶)
- عرض Container: ۱۴۴۰px، عرض Content: ۱۲۸۰px

### ویژگی‌های برنامه‌ریزی‌شده
Mega Menu، فیلتر محصول، Wishlist، Compare، Quick View، Recently Viewed، تنوع محصول (رنگ/سایز)، وضعیت موجودی، ماشین‌حساب زمان تحویل، PayPal Express Checkout، خرید مهمان (Guest Checkout)، نظرات مشتری، FAQ، بلاگ، خبرنامه، بنر رضایت کوکی (طبق GDPR)، دوزبانه (آلمانی + انگلیسی)، جست‌وجوی سریع با پیشنهاد لحظه‌ای.

### وضعیت فعلی این پروژه (۱۷ ژوئیه ۲۰۲۶)
- ✅ معماری فنی و هویت بصری تصمیم‌گیری شده
- ✅ **مخزن گیت‌هاب ساخته شد:** https://github.com/AS228630/outlet-moebel-shop (عمومی)
- ✅ **اسکلت اولیه پروژه push شد:** Next.js 16 + TypeScript + Tailwind CSS v4، سیستم طراحی کامل (رنگ‌ها/فونت‌ها/radius/سایه از روی لوگوی واقعی)، کامپوننت‌های پایه‌ی shadcn/ui-style (Button، Card — ساخته‌شده دستی چون CLI رسمی shadcn نیاز به دسترسی شبکه به `ui.shadcn.com` داشت که در sandbox موجود نبود)، فونت‌های self-hosted (Manrope + Inter از طریق `@fontsource`، چون `next/font/google` هم نیاز به شبکه‌ی گوگل داشت)، کامپوننت Header (نوار بالا + لوگو + جست‌وجو + آیکون‌ها + منوی دسته‌بندی)، بخش Hero صفحه‌ی اصلی با انیمیشن Framer Motion
- ✅ تست شده: `tsc --noEmit`، `eslint`، و `next build` کامل — همه بدون خطا
- ⏳ **قدم بعدی:** ساخت صفحات دسته‌بندی محصول، صفحه‌ی محصول، سبد خرید، اتصال Prisma+PostgreSQL، احراز هویت، Stripe

جزئیات کامل معماری در `README.md` همون مخزن هم مستند شده.

---

عطاالله جان، این فایل الان در ریشه‌ی مخزن گیت‌هاب (`PROJECT_GUIDE.md`) قرار داره. برای شروع هر چت جدید:

1. یا این فایل رو مستقیم از گیت‌هاب کپی کن و بفرست،
2. یا فقط بگو «فایل PROJECT_GUIDE.md رو از گیت‌هاب بخون» تا با clone کردن ریپو خودش خونده بشه.

**در پایان هر جلسه‌ی کاری، این فایل باید به‌روزرسانی و push بشه** — یه بخش جدید به «تاریخچه‌ی تغییرات» (بخش ۱۰) اضافه بشه، بخش «قدم‌های باقی‌مانده» (بخش ۱۱) با وضعیت جدید هماهنگ بشه، و اگه روی پروژه‌ی Outlet Möbel (بخش ۱۳) کار شد، وضعیتش هم به‌روز بشه.
